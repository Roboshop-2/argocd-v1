apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: roboshop-prod
  namespace: argocd
  # The finalizer enables cascading deletes (deleting this app deletes its resources)
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  # The Argo CD project this application belongs to
  project: default

  source:
    repoURL: https://github.com/Roboshop-2/argocd-v1.git
    targetRevision: main
    path: applications/roboshop-prod

  # Destination configuration pointing to the target cluster and namespace
  destination:
    server: https://kubernetes.default.svc
    namespace: roboshop-prod

  syncPolicy:
    automated:
      prune: true       # Automatically deletes resources no longer in Git
      selfHeal: true  